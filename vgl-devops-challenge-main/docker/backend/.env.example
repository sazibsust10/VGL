# Backend environment configuration
# Driver can be 'sqlite' or 'mysql'
DB_DRIVER=sqlite

# SQLite settings (used when DB_DRIVER=sqlite)
DB_PATH=data/dev.db

# MySQL Production settings (used when DB_DRIVER=mysql)
DB_HOST=186.166.166.166
DB_PORT=3306
DB_NAME=app
DB_USER=app
DB_PASS=vgl_SQL1!

# Server
HTTP_HOST=0.0.0.0
HTTP_PORT=8080
